services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: mentorque-api
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      # Neon pooled DB connection (SSL + pgbouncer + timeout)
      DATABASE_URL: "postgresql://neondb_owner:npg_oQdVp89IRbzv@ep-small-hall-a1kchcgr-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&pgbouncer=true&connect_timeout=15"
      PRISMA_CLIENT_ENGINE_TYPE: binary
      JWT_SECRET: supersecretjwt2024mentorque
      PORT: 5000
      NODE_ENV: production
    volumes:
      - ./backend/src:/app/src:ro

# No local postgres volumes — using Neon cloud DB only
